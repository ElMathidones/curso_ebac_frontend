function multiplicar(numero1: number, numero2: number): number {
    return numero1 * numero2;
}

function saudar(nome: string): string {
    return "Olá, " + nome;
}

console.log(multiplicar(4, 5));
console.log(saudar("Mathias"));